--- a/cmd/mcp_handlers.go
+++ b/cmd/mcp_handlers.go
@@ -734,7 +734,7 @@
 
 func currentUserID() int {
 	auth, err := wanderlog.LoadCredentials()
-	if err != nil || auth.UserID == "" {
+	if err != nil || auth == nil || auth.UserID == "" {
 		return 0
 	}
 	userID, err := strconv.Atoi(auth.UserID)
